<?php
// operator aritmatika => operasi matematika
    // +$variable => data variable menjadi positif
    // -$variable => data variable menjadi negatif
        // $variable + $variable => penambahan
        // - => pengurangan
        // * => perkalian
        // / => pembagian
        // % => sisa bagi(modul)
        // ** => pangkat

    $result = 10 + 10;
    var_dump($result);

    $resultNegatif = -$result;
    var_dump($resultNegatif);

    $resultModule = 100 % 2;
    var_dump($resultModule);